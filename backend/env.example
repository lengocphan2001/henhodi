# Server Configuration
PORT=5000

# API Base URL (for generating image URLs)
# In production, set this to your domain: https://chumgaiphuquoc.com
# If not set, will use request host or default to http://localhost:3000
API_BASE_URL=https://chumgaiphuquoc.com

# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=henhodi

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_here_change_this_in_production

# Optional: CORS Configuration
CORS_ORIGIN=http://localhost:3000 